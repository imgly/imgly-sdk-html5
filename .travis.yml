language: node_js

node_js:
  - "4.1.0"

cache:
  directories:
    - node_modules

after_script:
  - npm run coveralls

sudo: false

addons:
  apt:
    packages:
    - libcairo2-dev
    - libjpeg8-dev
    - libpango1.0-dev
    - libgif-dev
